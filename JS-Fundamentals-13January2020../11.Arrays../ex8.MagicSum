function magic(array,toGivenNumber){
    let pairArray = []
    let sum = 0;

    for(let i = 0;i < array.length;i++){
        for(let j = i+1;j < array.length ;j++){
           sum = Number(array[i]) + Number(array[j]);
           if(sum === toGivenNumber){
               //pairArray.push(array[i]);   

              // pairArray.push(array[j]);
            console.log(pairArray.join(' '),array[i],array[j])
           }
        }
    }
}
magic([1, 2, 3, 4, 5, 6],
    6
    )
