function sumOfDigits(number){
    let numForChange = number;

    let changedNum = numForChange.toString();
    let result = 0;

    for(let i = 1 ; i<= changedNum.length;i++)
    {
        result += parseInt(changedNum[i-1]);
    }
    console.log(result);

}
sumOfDigits(543)
